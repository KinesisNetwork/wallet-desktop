language: node_js
addons:
  apt:
    packages:
      - wine
      - fakeroot
      - dpkg
      - rpm
before_install:
  - npm install -g electron-forge
before_script:
  - npm run sass-compile
jobs:
  include:
    - stage: test
      script: electron-forge package --platform=linux
    - script: electron-forge package --platform=win32
    - stage: publish
      script: electron-forge publish --targets=squirrel,zip,deb,rpm --arch=x64
