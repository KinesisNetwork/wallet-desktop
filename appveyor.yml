environment:
  nodejs_version: 8
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install -g electron-forge
  - npm install
before_build:
  - ps: npm run sass-compile
build_script:
  - ps: electron-forge publish --target=s3
