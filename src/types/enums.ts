export enum View {
  create,
  settings,
  dashboard,
  payees,
}

export enum AccountPage {
  transfer = 'Transfer',
  sign = 'Sign & Verify',
}

export enum CreateWalletFormView {
  select,
  generate,
  import,
}
